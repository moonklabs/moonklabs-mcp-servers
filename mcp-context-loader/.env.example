# mcp-context-loader 환경변수
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# =============================================================================
# 필수 환경변수
# =============================================================================

# 로그 레벨 (debug, info, warn, error)
# 기본값: info
LOG_LEVEL=info

# =============================================================================
# 선택 환경변수
# =============================================================================

# Node.js 환경 (development, production, test)
# 기본값: development
NODE_ENV=development

# HTTP 서버 포트 (dev:http 모드에서 사용)
# 기본값: 3001
PORT=3001

# HTTP 서버 호스트
# 기본값: 0.0.0.0
HOST=0.0.0.0

# 프로젝트 루트 경로 (컨텍스트 파일 탐색용)
# 기본값: .
PROJECT_ROOT=.

# 캐시 TTL (초)
# 기본값: 300 (5분)
CACHE_TTL_SECONDS=300
