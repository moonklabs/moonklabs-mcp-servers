# Notion API 설정
# https://www.notion.so/my-integrations 에서 생성
NOTION_TOKEN=secret_xxxxxxxxxxxxxxxxxxxxx

# MKL작업 데이터베이스 ID
# Notion 페이지 URL에서 추출: https://notion.so/{workspace}/{database_id}?v=...
NOTION_TASK_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MKL스프린트 데이터베이스 ID (스프린트 조회용)
NOTION_SPRINT_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Inbox 데이터베이스 ID (문서 DB)
NOTION_INBOX_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# HTTP 서버 설정
PORT=3434
HOST=0.0.0.0

# 로그 레벨 설정
# 가능한 값: debug, info, warn, error, silent
# debug: 모든 로그 (요청 본문 포함)
# info: 일반 로그 (기본값)
# warn: 경고 이상
# error: 에러만
# silent: 로그 출력 안 함
LOG_LEVEL=info

# 사용자 이메일 도메인
# 각 도구에서 userId(이메일 앞부분)를 전체 이메일로 변환할 때 사용
# 예: EMAIL_DOMAIN=moonklabs.com → userId "hong" → "hong@moonklabs.com"
EMAIL_DOMAIN=moonklabs.com
